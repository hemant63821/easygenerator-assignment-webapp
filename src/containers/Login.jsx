import "./Login.scss";

export default function Login(props) {
  return (
    <div>
      {console.log("check here if navigated")}
      <div>check 1 2 3</div>
      chehck and test 123
    </div>
  );
}

// const mapStateToProps = state => ({
//     validUser: state.validateUser.error
// })

// export default connect(mapStateToProps, { validateUser })(Login)
